<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Kamera izni (levha tespiti için zorunlu) -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- Ekranı açık tut (sürüş modu) -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- Titreşim (kritik levha uyarısı) -->
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- Kamera donanım özelliği -->
    <uses-feature android:name="android.hardware.camera" android:required="true"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>

    <application
        android:label="TrafikAsistan"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:extractNativeLibs="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="portrait">

            <!-- Kamera iznini manifest düzeyinde belirt -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Flutter embedding -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
    </application>

    <!-- Android 12+ için kamera query izni -->
    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE"/>
        </intent>
    </queries>
</manifest>
